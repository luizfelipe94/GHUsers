# setup:
#   addons:
#     - plan: heroku-postgresql
#       as: DATABASE
#   config:
#     S3_BUCKET: my-example-bucket
build:
  docker:
    api: Dockerfile
    worker: worker/Dockerfile
  # config:
  #   RAILS_ENV: development
  #   FOO: bar
release:
  command:
    - ./deployment-tasks.sh
  image: worker